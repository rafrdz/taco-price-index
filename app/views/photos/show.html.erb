<p class="mb-2"><strong>URL:</strong> <%= @photo.url %></p>
<p class="mb-2"><strong>Taco:</strong> <%= @photo.taco_id %></p>

<%= link_to 'Edit', edit_photo_path(@photo), class: 'btn btn-primary' %>
<%= link_to 'Back', photos_path, class: 'btn btn-secondary ms-2' %>
