# Rename this file to `.env` and fill in your credentials
# These variables are loaded automatically in development by dotenv-rails.

# Google Maps JavaScript API key used by the Rails frontend
GOOGLE_MAPS_API_KEY=YOUR_GOOGLE_MAPS_API_KEY

# PostgreSQL (Docker) credentials – must match docker-compose.yml
POSTGRES_DB=tacos_db
POSTGRES_USER=tacos
POSTGRES_PASSWORD=tacos_password
